# FTP Sync 3.0 readiness

Status: IMPLEMENTING — shared per-image template preparation is integrated into
preview, transfer and conditional local reprocessing. Explicit variable editors and
linked-calendar activation admission are implemented; actual GUI verification,
live geocoding validation, face integration and candidate readiness remain incomplete.

- Coordinator: this local task, recurring heartbeat every 10 minutes.
- Automation ID: `ftp-sync-3-0-implementation-coordinator` (ACTIVE).
- Branch: `codex/version-3-0-plan`.
- Setup baseline: `d6661e2c14d831c2f40a897ce0a0cdcd911ae213`.
- Plan's original inspected source: `a1d8f37` (2.9.2, build 37).
- Consecutive cycles with no substantive progress: 0.
- Candidate: none; see `Testing/3.0-candidate.json`.
- User final manual testing: not started.
- Latest implementation evidence: [2026-09-12 face metadata and component foundation](Testing/2026-09-12-M4-Face-Metadata-and-Component-Foundation.md).
- Latest app-code commit: `f6f8407`; focused face writer/component suite 14 passed,
  zero failures. The prior clean full suite at `da3579e` had 1,035 passed, 15 opt-in skips,
  zero failures (1,050 discovered). Strict directory-package import/export now
  preserves a hash-bound Photo Agent editor payload byte-for-byte while retaining a
  separate recognition-core revision. The admitted v3 runtime owns a cancellable
  People Library controller and settings panel for import, exact export, summary and
  deselection. The panel also admits bounded ZIP32 packages after complete archive
  validation. Model/runtime recognition, companion proof and calibrated face
  workflows remain open. Accepted names can now be appended losslessly to XMP
  Person Shown and, by explicit caller choice, intact Keywords. A signed optional
  model-component installer foundation is present but has no production key/URLs,
  exposes no runtime model, and does not enable recognition.
  App remains 2.9.2 (37); installed copy unchanged.
- Latest cycle: no desktop use while companion work may contend. FTP Sync committed
  the face metadata writer path and an isolated signed AuraFace component foundation.
  Independent review findings covering stable dedupe, sidecar output limits, target
  membership, URL lifetime, rollback recovery, storage admission and concurrent
  cleanup were fixed. Focused tests pass. A broad run was invalidated by the current
  test host denying existing suites access to `/private/tmp` (156 failures, 898 passes,
  15 opt-in skips); it is recorded as environment evidence rather than a regression pass.
  A separate extraction audit found that the pinned model says BGR/Torch 2.12 while
  its manifest and Swift preprocessor say RGB/Torch 2.8. The companion coordinator
  received this compatibility blocker; recognition publication stays disabled pending
  a corrected contract or pinned reference-vector evidence.
  Neither app currently has a shared App Group entitlement. Automatic sync remains an
  opt-in one-writer/shared-pointer design; no native, real-model, real-face or manual
  case advanced.
- Native computer selection of a separately identified, isolated QA app timed out after
  722.8949 seconds despite a 20-second timeout. Inventory works; no actual app GUI pass
  is claimed. The owned QA process was closed; pre-existing processes were left alone.
- A changed native path built a separately identified ad-hoc QA app and UI runner.
  Build-for-testing passed; two editor tests could not initialize because XCTest
  reported authentication canceled (exit 65). No UI test ran. Final process inventory
  had no owned QA app/runner; pre-existing PID 59841 remained untouched. Retest on an
  authorized desktop without bypassing authentication; independent implementation continues.

## Milestones

| Milestone | Implementation | Verification |
| --- | --- | --- |
| M0 compatibility/baseline | Provenance, executable probes and proposed contracts recorded; two measured scan-cost fixes committed | Local probes, 100,000-file checkpoint baseline and full app suite pass; supported-OS, installer, accuracy and complete performance gates open |
| M1 resolved changes/variables | Persisted context, shared per-image resolution, proposed-value preview, explicit variable editors and guarded transfer/reprocess implemented | Integrated regression suite and independent review; actual activated GUI workflows remain untested |
| M2 reverse geocoding | Offline job settings, shared lookup, independent place policies, optional-assignment transfer/reprocess, controls, preview and audit implemented; explicit Apple selection/consent and production routing implemented | Full suite passes; real offline database, injected Apple matrix and one native MapKit request tested; supported-OS, live failure/recovery and actual UI/location matrix remain open |
| M3 shared-template compatibility | Startup/storage admission, package3, separate server namespace, namespace-aware sharing and explicit classic-calendar migration/recovery/archival implemented | Swift and PHP/MySQL suites pass, including actual old-server rollback/races; native/live migration, supported-OS and older-process exclusion remain open |
| M4 local face recognition | Validated matcher/codec, strict manifest, immutable snapshots, lossless editor extension, exact directory/ZIP import, face-name metadata append and isolated signed model-component foundation implemented; production component configuration and recognition runtime pending | Focused writer/component suite 14 passed plus prior package/matcher evidence and independent review; schema-2 companion artifacts, native settings flow, calibration and actual-image/runtime evidence remain open |
| M5 recovery/performance/end-to-end | Not started | Not run |
| M6 candidate and release checks | Not started | Not run; release awaits user |

## Next actions

1. Observe the new startup/source-selection/recovery UI with isolated fixtures and
   validate conflict handling. Native selection still stalls after a changed launch and
   unique QA identity; investigate a materially different observation path before retrying.
   Same-bundle process detection and acknowledgement support user-controlled exclusion,
   but cannot prevent an uncooperative 2.9 relaunch. Active cancellation is not draining.
   Define and verify later-launch job restoration: the current gate preserves saved launch
   choices while every admitted session remains paused until explicit user actions.
   Per-image context now uses strict original capture metadata, saved fallback zones and
   frozen processing dates; omissions are audited and prevent processed-source removal.
   Observe actual preview/transfer/reprocess and recovery behavior, plus the new typed
   editor sheets, saved-zone chooser, existing/proposed comparison and audit detail
   disclosure. These controls are implemented with regression evidence; their native
   behavior remains unverified. Keep protocol-2 calendar activation
   blocked until server-enforced protocol3 sharing is ready.
   Follow Testing/3.0-Activation-Integration-Checklist.md for remaining boundaries.
2. Extend the delivery baseline to representative image bursts and select measured
   memory/throughput/cancellation budgets. Keep the five-sample checkpoint-only result
   distinct from the final-commit smoke; perform a controlled final five-sample run.
3. Update Photo Agent's model packager to canonical schema 2 and deterministic ZIP32,
   choose a dedicated distribution key and fixed hosts, then regenerate/re-sign artifacts.
   Resolve the BGR/RGB contract before adding any model consumer. Arrange supported-OS
   execution and real-model labeled fixtures; verify the installer lifecycle and companion
   exporter contract without racing its coordinator.
4. Verify actual Apple behavior on both OS adapter branches and the new provider and
   import consent dialogs on an authorized desktop. Provider selection/routing is now
   implemented with injected tests and one successful native MapKit fixture; older-OS,
   live failure/recovery and native UI remain unverified. Observe
   standalone/combined preview, transfer and reprocess with real RAW/XMP. Add offline
   rural/coastal/border fixtures and measured 50 km policy validation. Verify system
   Expat on supported OS releases and measure validation overhead. Keep pinned
   SwiftMediaMetadata 2.0.0. M5 must add settings/library/source processing fingerprints:
   same-source repeat-poll coverage does not prove settings-change staleness or legacy
   destination receipt bootstrap. Enabled schedules still require their original-arrival
   or source-modification context during combined reprocessing.
5. Verify M3 explicit classic-calendar migration on the actual app and disposable
   upgraded/old servers, including review, same-UUID lost-response recovery, confirmed
   rebind, abandonment and detach/rejoin. Implementation and injected persistence/network
   regressions pass; no native/live end-to-end acceptance is implied. Then prioritize
   M4 model installation and recognition integration
   against the pinned companion contract. The matcher, codec, strict manifest, atomic
   local snapshots, exact directory-package round trip and settings import/export are
   implemented without production acceptance defaults. Photo Agent must prove AuraFace v3 provenance or
   re-enroll legacy examples; arbitrary legacy archives cannot be relabeled compatible.
   These remain independent implementation work while native and supported-OS
   verification are unavailable. The ZIP importer is implemented; next resolve the
   pinned model's BGR/RGB contract discrepancy before extracting the runtime.
6. Refine checklist fixtures and exact UI labels as each feature lands. Preserve user
   results and candidate history; record only observed agent evidence.

## Dependencies to investigate (not yet proven blockers)

- Photo Agent has a separate active 3.0 coordinator and a required companion exporter
  contract. Coordinate shared source and serialize desktop use; do not overwrite its work.
- The local signed model and hosted descriptor/signature were verified; hosted archive delivery,
  installer behavior, actual-image accuracy and authorized labeled fixtures remain open.
- macOS 14 runtime testing, current supported macOS runtime, signing identity, PHP/MySQL
  environment, and test-only online geocoding. Missing prerequisites cannot count as passes.

## Setup validation

Completed 2026-09-09: coordinator activated, 42-case catalog created, independent
checklist/protocol review completed, five loopback persistence/access/concurrency tests
passed, JavaScript syntax checked, and actual browser save/reload/draft recovery observed.
See [setup verification](Testing/2026-09-09-checklist-setup.md). All app-specific cases
remain unrun. The user result file has not been populated with synthetic testing data.
Setup changes are prepared as one coherent local commit; see Git history for identity.

## Stable-release maintenance before M0

On 2026-09-09 the user reported live-calendar network errors in installed 2.9.2.
Maintenance commit `d2a5f05` implements per-account retry backoff and cleaner
network diagnostics, with no 3.0 feature/version change. See
[the investigation and validation](2.9.2-Calendar-Network-Investigation.md).
The full unit suite passed (473 passed, 15 opt-in skips, zero failures). The actual
cause of the user's connectivity failures is not established; installed 2.9.2 was
not replaced. Preserve this fix while continuing the dependency-ordered 3.0 plan.

User connectivity follow-up on 2026-09-11: internet status during the reported errors
was unknown, although the Mac is usually connected. This does not establish a local
network or server cause; no additional credentials or live endpoint were inspected.
