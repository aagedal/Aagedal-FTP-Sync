# FTP Sync 3.0 readiness

Status: IMPLEMENTING — atomic opt-in bootstrap now composes migration/recovery,
lease ownership and both paused runtimes. Physical startup path preparation is tested.
Production scenes still use legacy startup; activation and candidate readiness remain incomplete.

- Coordinator: this local task, recurring heartbeat every 10 minutes.
- Automation ID: `ftp-sync-3-0-implementation-coordinator` (ACTIVE).
- Branch: `codex/version-3-0-plan`.
- Setup baseline: `d6661e2c14d831c2f40a897ce0a0cdcd911ae213`.
- Plan's original inspected source: `a1d8f37` (2.9.2, build 37).
- Consecutive cycles with no substantive progress: 0.
- Candidate: none; see `Testing/3.0-candidate.json`.
- User final manual testing: not started.
- Latest implementation evidence: [2026-09-10 atomic bootstrap](Testing/2026-09-10-M3-Atomic-Bootstrap.md).
- Latest app-code commit: `33e4b0a`; full suite 698 passed / 15 opt-in skips / zero
  failures. All 13 new bootstrap/path/debounce cases pass. A stale calendar echo found during
  integration was fixed separately in `ae2c3eb`.
  Unchanged template package at `c0de968`: 33 tests pass. The earlier 100,000-file
  FTP/SFTP combined-fix smoke remains recorded separately. App remains 2.9.2 (37);
  installed copy unchanged. These are development checks, not candidate gates.
- Initial Xcode test-session connection failures cleared on a later unchanged-source
  retry. Native computer selection again stalled (5897 seconds despite a 20-second
  timeout); surface inventory works, but no actual app GUI pass is claimed.

## Milestones

| Milestone | Implementation | Verification |
| --- | --- | --- |
| M0 compatibility/baseline | Provenance, executable probes and proposed contracts recorded; two measured scan-cost fixes committed | Local probes, 100,000-file checkpoint baseline and full app suite pass; supported-OS, installer, accuracy and complete performance gates open |
| M1 resolved changes/variables | Core, strict capture reader, activation values and safe legacy IPTC Unicode writes implemented; production paths share literal snapshots; activated UI/context integration pending | Package and full app suite results above; independently reviewed; actual activated workflows remain untested |
| M2 reverse geocoding | Not started | Not run |
| M3 shared-template compatibility | Atomic opt-in bootstrap, physical paths, paused factories, lease and runtime map admission implemented; production scenes not switched | Full suite passes; continuous older-process exclusion and recovery/source selection UI remain open; obsolete credential collection disabled by admission |
| M4 local face recognition | Not started | Not run |
| M5 recovery/performance/end-to-end | Not started | Not run |
| M6 candidate and release checks | Not started | Not run; release awaits user |

## Next actions

1. Replace eager App.main stores with a gated loading/ready/recovery shell across all
   menu, window and Settings scenes; publish the already-composed bootstrap runtime.
   LSUIElement startup needs an independent attention label and explicit recovery-window
   action without a default AppStore. Provide explicit source/backup choices and frozen
   recovery; never offer implicit fresh state for damaged COMMITTED storage.
   Establish continuous older-writer exclusion before calling bootstrap. Same-bundle
   running-app detection/rechecks can support user-controlled exclusion but cannot stop
   an uncooperative 2.9 process relaunching; the lease alone is insufficient. Keep lease
   and Runtime alive for the full writer lifetime, and initial migration paused for review.
   Physical path preparation requires existing trusted parent directories; establish the
   sandbox's Application Support parent on a genuinely new installation. Preserve test
   isolation, saved launch choices and disabled obsolete-credential collection.
   Follow the storage inventory and proposed calendar contract before user-facing activation.
   Integrate strict EXIF context reading with persisted zone,
   partial-completion audit/source-removal guards, and activated preview/transfer wiring.
   Enforce server source limits in persistence/transport adapters; preserve legacy literals.
   Follow Testing/3.0-Activation-Integration-Checklist.md before accepting any persisted
   activation marker: both literal execution paths, copies/merges, packages and calendars
   must be guarded together.
2. Extend the delivery baseline to representative image bursts and select measured
   memory/throughput/cancellation budgets. Keep the five-sample checkpoint-only result
   distinct from the final-commit smoke; perform a controlled final five-sample run.
3. Arrange supported-OS execution and real-model labeled fixtures; verify model hosting,
   installer lifecycle and the companion exporter contract without racing its coordinator.
4. Implement bounded geocoding and shared processing context in dependency order. Keep
   SwiftMediaMetadata 2.0.0 initially: required APIs were verified without a major upgrade.
5. Refine checklist fixtures and exact UI labels as each feature lands. Preserve user
   results and candidate history; record only observed agent evidence.

## Dependencies to investigate (not yet proven blockers)

- Photo Agent has a separate active 3.0 coordinator and a required companion exporter
  contract. Coordinate shared source and serialize desktop use; do not overwrite its work.
- The local signed model and hosted descriptor/signature were verified; hosted archive delivery,
  installer behavior, actual-image accuracy and authorized labeled fixtures remain open.
- macOS 14 runtime testing, current supported macOS runtime, signing identity, PHP/MySQL
  environment, and test-only online geocoding. Missing prerequisites cannot count as passes.

## Setup validation

Completed 2026-09-09: coordinator activated, 42-case catalog created, independent
checklist/protocol review completed, five loopback persistence/access/concurrency tests
passed, JavaScript syntax checked, and actual browser save/reload/draft recovery observed.
See [setup verification](Testing/2026-09-09-checklist-setup.md). All app-specific cases
remain unrun. The user result file has not been populated with synthetic testing data.
Setup changes are prepared as one coherent local commit; see Git history for identity.

## Stable-release maintenance before M0

On 2026-09-09 the user reported live-calendar network errors in installed 2.9.2.
Maintenance commit `d2a5f05` implements per-account retry backoff and cleaner
network diagnostics, with no 3.0 feature/version change. See
[the investigation and validation](2.9.2-Calendar-Network-Investigation.md).
The full unit suite passed (473 passed, 15 opt-in skips, zero failures). The actual
cause of the user's connectivity failures is not established; installed 2.9.2 was
not replaced. Preserve this fix while continuing the dependency-ordered 3.0 plan.
