# FTP Sync 3.0 implementation coordinator

Authorized by the user on 2026-09-09: automate the full 3.0 implementation plan,
use agents, make reasonable local commits, test through the computer, and notify
when a candidate is ready for the user's final manual test before release.

## Authority and scope

`Documentation/3.0-Implementation-Plan.md` is the acceptance contract. Complete M0–M5
and the pre-release preparation and validation in M6. Do not reduce required scope,
raise the minimum OS, waive real-model validation, or label missing evidence as a pass.
Prepare a versioned, signed release candidate when signing is available; public
release, push, tag publication, notarization submission, and production deployment
remain outside this implementation authorization. The user's final manual acceptance
must precede release. Do not make purchases or redeem usage resets automatically.

Use disposable photos, folders, loopback servers and test calendars. Preserve existing
app data and credentials; isolate GUI test state before enabling automatic jobs.
Only delete test fixtures created for these runs. Do not transfer user photos to real
recipients. Never commit secrets, face libraries, private photos or private test notes.

Work in this checkout on `codex/version-3-0-plan` initially. Inspect current branch,
status and other active tasks each cycle. Preserve unrelated work; coordinate or
isolate conflicting changes. Do not reset, clean, or indiscriminately stage the tree.
Create a `codex/` implementation branch if needed, then record it in readiness.

## Working cycle

1. Read this protocol, the full plan, `Documentation/3.0-Readiness.md`, candidate
   identity in `Documentation/Testing/3.0-candidate.json`, and local results in
   `Documentation/Testing/3.0-results.local.json` if present. Inspect recent commits
   and reconcile completed work with actual source and acceptance evidence.
2. Select the next dependency-ordered milestone and finish substantial implementation
   or verification. Keep an explicit next-action list. If one dependency is unavailable,
   finish independent work; a probe failure need not stop unrelated development.
3. Use bounded sub-agents for independent implementation and review. Assign disjoint
   file ownership. The coordinator alone integrates and commits. Serialize Xcode builds
   sharing DerivedData and all desktop interactions. Delegate only where useful.
4. Implement coherent slices, inspect the diff, run meaningful focused checks, fix
   failures, and commit reviewed changes with precise messages. Stage only owned files.
   Do not wait for an entire milestone to commit a independently valid change. Mark
   plan checkboxes only when their stated criteria have evidence. Keep implementation
   completion distinct from verification completion.
5. Exercise the actual newly built app using computer-use tools. Inspect launch, normal
   UI workflows, errors, cancellation and relaunch persistence with disposable fixtures.
   Record observed outcomes and screenshots when useful. Unit tests, mocks and a build
   success do not substitute for UI observation or real-model calibration.
6. Reuse README, SECURITY.md and existing test/benchmark scripts. Run broad integrated
   checks at integration boundaries and for the final candidate; do not rerun unchanged
   suites on every wakeup. Record exact commands, exit status, source revision and dirty
   state, macOS/hardware, app path/build, fixture/model/library versions, metrics, and
   evidence paths in dated `Documentation/Testing/` reports. Put large logs/artifacts in
   ignored build directories and preserve concise durable reports with hashes as useful.
7. Update readiness with completed criteria, tested revisions, open defects, external
   dependencies, attempted remedies and next steps. Continue useful work within the run;
   the heartbeat resumes the same task after the run ends.

## Companion work and shared desktop

The adjacent `/Users/truls.aagedal/Developer/Aagedal-Photo-Agent` has its own active
3.0 coordinator. Inspect its current source/protocol and task status before extracting
code or arranging the exporter contract. Pin reusable source revisions and retain
notices. Prepare the companion exporter change in an isolated patch/checkout if write
access or concurrent ownership prevents integration; record the exact integration gate.
Do not overwrite its working tree, broaden its release scope, or claim its exporter
change is integrated merely because an FTP Sync fixture accepts the proposed format.

Before GUI work, inspect active tasks and the desktop. If another agent or the user is
operating the same desktop, do independent work first and retry later. Give only one
sub-agent GUI ownership. Do not interrupt unrelated apps or the Photo Agent coordinator.
Use an isolated browser tab for checklist verification. A desktop/session permission
block must be recorded accurately and revisited, not bypassed or treated as passing.

## Persistent checklist contract

Launch `python3 Scripts/serve-3.0-checklist.py` (or double-click
`Scripts/Open-3.0-Checklist.command`). The local HTTP server binds only 127.0.0.1.
Use the printed session URL. Keep it running while entering results. Restart it after
reboot to resume from the same JSON file. Never replace an unreadable results file with
empty state. The server locks the result file against concurrent servers and uses atomic
writes plus revision conflict checks. A stale tab must reload before another save.

- `3.0-cases.json`: versioned, stable case IDs and detailed instructions. Update as real
  UI labels and fixtures become available; add concrete fixture paths and evidence links
  before handoff. Do not weaken expectations to hide defects.
- `3.0-candidate.json`: tracked exact candidate identity (unique id, source commit,
  version/build, app path, readiness status, evidence and note). A changed app binary,
  source revision or material test semantics requires a NEW candidate id. Preserve
  previous result sets. Do not recycle the development id for a final candidate.
- `3.0-results.local.json`: ignored durable private observations. Schema 1; `runs` keyed
  by candidate id, each containing a candidate snapshot, separate `user` and `agent`
  case maps and an append-only history. Top-level `revision` increments on every save.
  Statuses: `not_run`, `passed`, `failed`, `blocked`. Every result has notes, environment,
  and UTC updatedAt. Missing entries mean not run. Skipping a required test is not passing.
- Agents must read user results on every run, preserve them, and address failures. Agents
  record only in the `agent` lane; never fill in or modify human acceptance. Prefer the
  checklist API/UI for saves. If direct file editing is necessary, stop the server first,
  preserve all lanes/history, increment revision and use an atomic replacement.
- User results remain on this Mac across sessions, not automatically across computers.
  The Export button creates a backup JSON copy. Restore only with the server stopped,
  retaining schema and all earlier runs. Keep private raw results out of Git; record
  sanitized findings and references in readiness.

## Candidate gate

READY_FOR_USER_TESTING requires every mandatory M0–M5 criterion and M6 preparation/check
complete, no unresolved blocking defect, exact candidate identity and all of:

- Relevant unit, full integration, release/security guards, signed UI smoke, vendored SSH,
  loopback remote transport and PHP/MySQL tests pass for the candidate source.
- Actual app UI evidence covers variables, geocoding, library/model lifecycle, matching,
  transfer and RAW/XMP integrity, source-removal guards, preview/reprocess, recovery,
  migration/sharing, accessibility and relaunch. Required supported OS and hardware
  evidence is present. Real-model accuracy and cold/warm performance meet M0 budgets.
- Every required agent checklist case has passing evidence for the final candidate;
  automated evidence can support infrastructure cases but cannot impersonate a UI test.
  Failed, blocked and unrun required gates prevent readiness. The user-requested final acceptance case `m6-004` has `userOnly: true`, is excluded
  from the agent gate, and must remain for the user after this handoff. All other 41
  required cases need agent evidence; final acceptance never substitutes for them.
- An independent reviewer checks scope coverage, code, evidence, candidate identity and
  outstanding risks; resolve findings and record a reasoned coordinator readiness decision.
- The checklist works in the browser, gives exact setup/steps/expected outcomes, and
  points to the tested build. User result entries remain untouched and initially unrun.

On success, save READY_FOR_USER_TESTING and the candidate identity, pause THIS heartbeat
through the automation tool, and notify the user once with the app, checklist launcher,
source/build identity and concise verification evidence. Final manual acceptance and
any distribution actions remain outstanding. Never call the app released at this point.

If no useful implementation or verification can progress for three consecutive cycles,
record BLOCKED with the smallest concrete user action, pause this heartbeat, and send one
blocker handoff. Count substantive progress only; rewriting notes or retrying unchanged
failures does not reset the counter. New independent work should continue before stopping.
Ordinary progress belongs in repository records. Stay quiet for ordinary continuation and
unchanged non-actionable states; notify for readiness, a material failure or a blocker
requiring user action. Update the existing automation by ID; never create duplicates or
manually edit scheduler TOML. Once paused, wait for user continuation before resuming.
