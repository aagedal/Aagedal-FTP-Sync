# 2.9.2 client against the protocol-3 server

On 2026-09-22, the unmodified calendar integration test from the 2.9.2 release
source (`a1d8f37a9ea5d3e1b802dddcf775c1323e97051d`) passed against the current
protocol-3 PHP server. The source was extracted to `/private/tmp/aftpsync-292-source`
and compiled in a separate DerivedData directory. The test's loopback-only
transport uses the 2.9.2 request encoder, protocol-2 headers and response decoder;
its device stores and credentials are temporary fixtures.

```sh
mkdir -p /private/tmp/aftpsync-292-source
git archive a1d8f37 | tar -x -C /private/tmp/aftpsync-292-source
docker compose -p aftpsync-292-compat-20260922 \
  -f Server/MetadataSync/tests/compose.yaml \
  -f Server/MetadataSync/tests/compose-native.yaml \
  up -d --build --wait database server
cd /private/tmp/aftpsync-292-source
TEST_RUNNER_AFTPSYNC_METADATA_TEST_URL=http://127.0.0.1:18087/index.php \
  xcodebuild test -project 'Aagedal FTP Sync.xcodeproj' \
  -scheme AagedalFTPSync -destination 'platform=macOS' \
  -derivedDataPath /private/tmp/aftpsync-292-compat-derived \
  CODE_SIGNING_ALLOWED=NO -parallel-testing-enabled NO \
  -only-testing:AagedalFTPSyncTests/MetadataCalendarCoordinatorTests/testTwoDevicesAgainstPHPServer
```

Result: **one test passed, zero failures or skips** on Xcode 27.0. The test
registered a disposable first device, joined two invited devices, merged offline
edits, resolved a conflict and checked a date-limited editor. The result bundle is
`/private/tmp/aftpsync-292-compat-derived/Logs/Test/Test-AagedalFTPSync-2026.09.22_23-35-18-+0200.xcresult`.
The log is `/private/tmp/aftpsync-292-compat-xcodebuild.log` (SHA-256
`aa9f273ba84f7e19d35f1cc9270b0fbeb11d5caea99a52822b8213c7ad0622f1`).
The Docker project was stopped and removed after the test.

A genuine 2.9.2 (37) app bundle and archive are available locally, including the
2026-09-09 Xcode archive. The installed app was not launched and its container was
not read. This test proves the historical client's legacy-calendar path works with the
new server; it does not execute the archived signed binary against a template
calendar or close native migration, cached-client rejection, or supported-OS gates.
The current server's separate PHP integration suite covers protocol-2 rejection of
schema-free writes to template calendars at matching and stale revisions.
