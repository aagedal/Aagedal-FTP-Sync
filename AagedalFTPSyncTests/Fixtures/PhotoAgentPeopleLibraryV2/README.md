These four base64 files are the nonprivate People Library schema 2 golden package
from Aagedal Photo Agent commit `45c5ed1`, at
`Aagedal Photo Agent Tests/Fixtures/PeopleLibraryV2/`. The FTP Sync test decodes
the exact producer bytes and verifies import, export, and reimport. This is a
shared-format regression fixture, not a real-face recognition sample or a
schema 3 crop-bearing package.
